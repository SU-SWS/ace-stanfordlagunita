project:
  machine_name: library
  human_name: library
  local:
    protocol: http
    hostname: library.loc
  profile:
    name: sul_profile
drush:
  aliases:
    local: library.local
    remote: library.prod
drupal:
  db: {  }
