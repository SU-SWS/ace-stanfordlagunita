{#
/**
 * @file
 * Default theme implementation to display three column layout.
 *
 * Available variables:
 * - content: The content for this layout.
 * - attributes: HTML attributes for the layout wrapper.
 *
 * @ingroup themeable
 */
#}

{# Show heading in editor preview #}
{% if settings.heading %}
  <{{ settings.heading_level|default('h2') }}>
    {{ settings.heading }}
  </{{ settings.heading_level|default('h2') }}>
{% endif %}

{# Include the template with modified attributes #}
{% include "@stanford_layout_paragraphs/layouts/three_column/layout-paragraphs-three-column.html.twig" %}