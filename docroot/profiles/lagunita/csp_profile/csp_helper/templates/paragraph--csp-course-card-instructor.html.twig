{#
/**
 * @file
 * Maps the Course Card Instructor paragraph onto the matching component.
 *
 * Variables:
 * - instructor_name: plain text Name value.
 * - instructor_title: plain text Title value.
 * - instructor_url: resolved URL for the instructor's profile, if any.
 */
#}
{%
  set classes = [
    'paragraph',
    'paragraph--type--' ~ paragraph.bundle|clean_class,
    view_mode ? 'paragraph--view-mode--' ~ view_mode|clean_class,
    not paragraph.isPublished() ? 'paragraph--unpublished',
  ]
%}
{{ include('csp_helper:course_card_instructor', {
  attributes: attributes.addClass(classes),
  name: instructor_name,
  title: instructor_title,
  url: instructor_url,
  headshot: content.csp_instructor_headshot,
}, with_context = false) }}
